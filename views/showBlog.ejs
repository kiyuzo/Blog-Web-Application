<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ramul's Life Story</title>

    <link rel="stylesheet" href="styles.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&display=swap" rel="stylesheet">
</head>
<body>
    <!-- HEADER -->
    <%- include("partials/header.ejs") %>

    <!-- BLOG -->
     <div>
        <h1> <%= blog.title %></h1>
        <div>
            <span><%= blog.date %></span>
            <span><%= blog.author %></span>
        </div>
        <% blog.tags.forEach(function(tag) { %>
            <span class="tag"><i class="fas fa-tag"></i> <%= tag %></span>
            <% }); %>
        <p><%= blog.content %></p>
        
        <!-- Edit and Delete buttons -->
        <div>
            <a href="/blog/edit/<%= blogId %>" title="Edit Post">
                <img class="icon" src="/icons/edit.png" alt="edit icon">
                Edit
            </a>
            <form action="/blog/delete/<%= blogId %>" method="post" style="display: inline;">
                <button type="submit" title="Delete Post" onclick="return confirm('Are you sure you want to delete this post?');">
                    <img class="icon" src="/icons/trash.png" alt="trash icon">
                    Delete
                </button>
            </form>
        </div>
     </div>

     <a href="/blog">Back to blogs</a>

    <!-- FOOTER -->
    <%- include("partials/footer.ejs") %>
</body>
</html>