<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ramul's Life Story</title>

    <link rel="stylesheet" href="styles.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,200..800;1,200..800&display=swap" rel="stylesheet">
</head>
<body>
    <!-- HEADER -->
    <%- include("partials/header.ejs") %>

    <!-- BLOG -->
    <div class="blogs">
        <div class="blog-content">
            <h1><%= blog.title %></h1>
            <div class="blog-meta">
                <span><i class="fas fa-calendar"></i> <%= blog.date %></span>
                <span><i class="fas fa-user"></i> <%= blog.author %></span>
            </div>
            <div class="blog-tags">
                <% blog.tags.forEach(function(tag) { %>
                    <span class="tag"><i class="fas fa-tag"></i> <%= tag %></span>
                <% }); %>
            </div>
            <p class="blog-text"><%= blog.content %></p>
            
            <!-- Edit and Delete buttons -->
            <div class="blog-actions">
                <a href="/blog/edit/<%= blogId %>" title="Edit Post" class="btn-secondary">
                    <img class="icon" src="/icons/edit.png" alt="edit icon">
                    Edit
                </a>
                <form action="/blog/delete/<%= blogId %>" method="post" style="display: inline;">
                    <button type="submit" title="Delete Post" class="btn-danger" onclick="return confirm('Are you sure you want to delete this post?');">
                        <img class="icon" src="/icons/trash.png" alt="trash icon">
                        Delete
                    </button>
                </form>
            </div>
        </div>

        <div class="blog-navigation">
            <a href="/blog" class="btn-primary">← Back to blogs</a>
        </div>
    </div>

    <!-- FOOTER -->
    <%- include("partials/footer.ejs") %>
</body>
</html>